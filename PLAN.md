# 小行星 ISAR 几何重建损失建模计划

## 1. 当前目标

为基于 NeuS 的小行星 ISAR 三维重建建立与真实雷达处理流程一致的图像似然损失，使网络输出的平均目标功率能够在长时间非相干积累、低信噪比和热噪声背景下得到合理约束。

当前阶段先完成模型分析和方案确定，不立即修改训练代码。现阶段倾向于：当真实图像经过较多次独立非相干积累、目标散斑已基本被平均，仅剩热噪声起伏时，采用异方差高斯 NLL；若有效积累次数较小且需要严格建模，则采用多视非中心卡方 NLL。

## 2. 已确认的物理与统计结论

### 2.1 当前合成数据

- `public_data/1999JV6/generate_isar.py` 当前按面元功率直接相加，没有为各面元生成 `0~2π` 随机相位，也没有进行复振幅相干求和。
- 当前合成器将无噪声功率写成确定性复信号幅度，再加入 I/Q 两路高斯热噪声并取功率。
- 当前保存的图像只进行了一次功率检测，没有实现多个独立子 look 的非相干平均。
- 当前合成数据严格对应单视非中心卡方功率模型，而不是多视 Gamma 散斑模型。

### 2.2 背景噪声评估

- 已分析 18 幅图像四周 10% 的背景区域。
- 14 幅图像按 AIC 选择指数分布，4 幅选择 Gamma 分布。
- 合并归一化背景统计量：
  - `variance / mean^2 = 1.0289`；
  - `mean / median = 1.4602`；
  - 偏度 `1.9903`；
  - 超额峰度 `5.9137`。
- 上述结果接近单视指数功率噪声的理论值，当前数据的等效视数约为：

  ```text
  L_eff = mean^2 / variance ≈ 0.972 ≈ 1
  ```

- 当前合成数据没有表现出明显的多视非相干平均。

### 2.3 真实数据目标场景

- 真实小行星雷达图像通常可能由多个短相干处理段形成，再对各段功率进行长时间非相干积累。
- 长时间积累后，如果目标散斑已经基本被平均，可以将目标看成确定性平均功率，主要保留接收机热噪声。
- 非相干积累时间不能直接作为 Gamma 或非中心卡方模型中的 `L`；应使用独立子积累段的有效数量 `L_eff`。
- 如果子 look 相关、窗口重叠或目标散斑未充分去相关，则 `L_eff` 小于名义积累次数。

## 3. 候选损失模型

### 3.1 当前单视非中心卡方 NLL

令：

```text
x   = 网络预测的无噪声目标功率
y   = 观测功率
P_n = 每帧背景平均噪声功率
```

单视模型为：

```text
p(y | x, P_n)
= 1/P_n * exp(-(y+x)/P_n) * I0(2*sqrt(x*y)/P_n)
```

适用条件：确定性复信号、单次功率检测、只有复高斯热噪声。

### 3.2 Gamma NLL

若不同 look 的随机相位和散斑独立，网络预测平均信号功率，则：

```text
mu = x + P_n
y ~ Gamma(L_eff, mu / L_eff)
```

与网络参数有关的 NLL 为：

```text
loss = L_eff * (log(mu) + y / mu)
```

适用条件：残余散斑仍按指数/Gamma 功率模型描述。`L_eff = 1` 时退化为指数 NLL。

### 3.3 异方差高斯 NLL（当前真实场景的优先候选）

若散斑已基本平均，仅保留确定性目标功率和热噪声，则多次独立功率平均满足：

```text
mu       = x + P_n
variance = (P_n^2 + 2*x*P_n) / L_eff
```

当 `L_eff` 较大时，可近似为：

```text
y ~ Normal(mu, variance)
```

对应 NLL：

```text
loss = 0.5 * (log(variance) + (y - mu)^2 / variance)
```

必须保留 `log(variance)`，因为方差依赖预测功率 `x`。

### 3.4 多视非中心卡方 NLL

若散斑可以忽略，但 `L_eff` 不足以支持高斯近似，则使用严格的 `2*L_eff` 自由度多视非中心卡方分布。该模型物理上最严格，但需要稳定计算 `I_(L-1)`，实现和数值测试复杂度最高。

## 4. 已实现内容

- [x] ISAR 数据集读取与线性非负功率张量输出。
- [x] 基于每幅图四周 10% 背景均值估计 `P_n`。
- [x] 全数据集逐帧噪声功率估计 `frame_noise_power`。
- [x] 背景分布统计、AIC 对比、KS 指标与直方图输出。
- [x] 指数、Gamma、高斯、对数正态和 Student-t 分布对比。
- [x] 当前单视非中心卡方 NLL 主体公式。
- [x] 可学习的全局正增益 `exp(log_image_gain)`。
- [x] 图像损失与 Eikonal 损失的训练接入。
- [x] TensorBoard 中记录噪声功率、图像增益和预测功率均值。
- [x] checkpoint 中保存全局图像增益。

## 5. 当前实现中已知问题

- [ ] 当前非中心卡方 NLL 在 `predicted = 0` 时会产生 `NaN/-inf` 梯度。
- [ ] 完整 NLL 中的 `log(P_n)` 常数项未记录；虽然当前固定 `P_n` 时不影响网络梯度，但不利于跨帧比较完整似然。
- [ ] 配置文件尚未显式声明 `gain_learning_rate`、`noise_border_fraction` 和 `noise_likelihood_min`。
- [ ] 旧 checkpoint 不包含 `log_image_gain`，当前加载逻辑不向后兼容。
- [ ] 旧 optimizer 只有一个参数组，新 optimizer 有两个参数组，旧优化器状态可能无法直接恢复。
- [ ] 当前验证图没有使用已学习的全局图像增益，只分别按最大值归一化显示。
- [ ] 当前合成数据与计划中的真实长时间非相干积累模型不一致。

## 6. 待确认信息

- [ ] 真实图像保存的是多个功率 look 的求和还是平均。
- [ ] 长时间积累被划分为多少个短相干处理段。
- [ ] 子积累段是否重叠，使用了什么时间窗或频率窗。
- [ ] 是否保留了非相干积累前的子 look 图像。
- [ ] 真实处理流程是否已经减去背景噪声均值。
- [ ] 不同子 look 的目标散斑是否基本去相关。
- [ ] `L_eff` 是由处理参数给定，还是需要从真实数据估计。
- [ ] 真实图像在积累期间是否完成距离–多普勒对齐。
- [ ] 积累期间小行星姿态变化是否足以改变平均功率图。

## 7. 待实现内容

### 7.1 损失接口

- [ ] 增加统一的功率 NLL 接口，明确输入均为线性功率：`pred_signal_power`、`target_power`、`noise_power`、`effective_looks`。
- [ ] 增加可配置的损失类型，例如：

  ```text
  train.power_loss = heteroscedastic_gaussian
  ```

- [ ] 优先实现异方差高斯 NLL。
- [ ] 保留单视非中心卡方 NLL作为当前合成数据基线。
- [ ] 根据需要实现严格的多视非中心卡方 NLL。
- [ ] Gamma NLL只作为残余散斑不可忽略时的对照模型。

### 7.2 参数与配置

- [ ] 在配置文件中加入 `effective_looks`。
- [ ] 明确 `effective_looks` 是固定值、逐帧值还是由数据估计。
- [ ] 加入 NLL 数值稳定参数 `loss_epsilon`。
- [ ] 为全局增益设置合理初始化、学习率和有限性检查。
- [ ] 明确输入为非相干平均功率还是非相干累加功率，并统一尺度。

### 7.3 数据与生成器

- [ ] 若需要构造与真实处理一致的合成数据，将长积累划分为多个短相干 look。
- [ ] 每个 look 独立生成 I/Q 热噪声。
- [ ] 根据最终物理假设决定是否生成面元随机相位和目标散斑。
- [ ] 对各 look 分别取功率后再求和或平均。
- [ ] 在元数据中保存名义 look 数、积累方式和随机种子。
- [ ] 同时保存浮点功率数据，避免只使用 8 位 PNG 进行概率建模。

### 7.4 训练与日志

- [ ] 记录 `L_eff`、预测均值 `mu`、预测方差和标准化残差。
- [ ] 检查每一步 loss、梯度和参数是否为有限值。
- [ ] 分别记录完整 NLL 和去常数项 NLL。
- [ ] 验证时应用训练得到的图像增益，并输出线性功率误差统计。

## 8. 验收标准

- [ ] 所有合法的零背景、零预测和强目标输入均产生有限 loss 和有限梯度。
- [ ] 异方差高斯模型的经验均值和方差与理论公式一致。
- [ ] 当 `x << P_n` 时，损失正确退化为近似固定方差的加权 MSE。
- [ ] 当 `L_eff` 增大时，预测方差按 `1/L_eff` 缩小。
- [ ] 使用已知真值生成的合成数据时，真值参数附近的平均 NLL最低。
- [ ] 新损失能够完成短程训练，不出现 NaN、Inf 或增益发散。
- [ ] 与 L1、当前单视非中心卡方和 Gamma NLL进行相同初始化的对照实验。
- [ ] 比较验证图、SDF、mesh 稳定性和不同随机种子下的几何一致性。

## 9. 风险与注意事项

- 若实际散斑没有被充分平均，异方差高斯热噪声模型会低估强目标区域方差。
- 若错误使用 Gamma NLL，则可能多引入 `x^2/L` 方差并降低强散射区域的几何约束。
- 若错误使用名义 look 数而非 `L_eff`，NLL会对观测过度自信。
- 若积累期间平均功率随姿态明显变化，简单的同分布多 look 模型不再严格成立。
- 如果 `P_n`、全局增益和渲染功率尺度没有统一，损失参数将失去物理意义。
- 当前工作区中的 `isar_runner.py` 和 `scripts/debug_noise_floor.py` 已有未提交修改，后续编辑必须保留这些已有工作。

## 10. 当前决策

1. 不把积累时间或脉冲数量直接作为 `L`。
2. 使用真实处理流程或真实子 look 估计 `L_eff`。
3. 对真实长时间非相干积累、散斑可忽略的数据，优先采用异方差高斯 NLL。
4. 对当前未进行多 look 平均的合成数据，继续使用单视非中心卡方基线。
5. 在完成 `L_eff` 和积累方式确认前，不正式替换训练损失。

## 11. 下一步

优先获得或确认真实数据处理中的以下三个参数：

```text
1. 子相干积累段长度；
2. 非相干平均的名义次数；
3. 最终图像是功率求和还是功率平均。
```

随后使用真实背景或非相干积累前的子 look 估计 `L_eff`，再最终决定使用异方差高斯 NLL还是严格的多视非中心卡方 NLL。
